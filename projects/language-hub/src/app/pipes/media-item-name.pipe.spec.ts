import { MediaItemNamePipe } from './media-item-name.pipe';

describe('MediaItemNamePipe', () => {
  it('create an instance', () => {
    const pipe = new MediaItemNamePipe();
    expect(pipe).toBeTruthy();
  });
});
